import { buildAccordion } from "../ve-components/Accordion";

export const name = 'accordion';

export const component = {
    title: 'Accordion',
    category: 'List',
    fields: buildAccordion('accordion')
};